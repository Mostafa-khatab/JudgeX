#include <iostream>
#include <string>
using namespace std;
int main() {
    int t;
    if (!(cin >> t)) return 0;
    while (t--) {
        int n, k;
        cin >> n >> k;
        string s;
        cin >> s;
        cout << "YES " << n << " " << k << endl;
    }
    return 0;
}
