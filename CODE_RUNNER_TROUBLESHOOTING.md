# 🔧 Code Runner - حل المشاكل

## ✅ تم تحسين معالجة الأخطاء!

الآن Code Runner يعطي رسائل خطأ واضحة ومفيدة.

---

## 🐛 المشاكل الشائعة وحلولها

### 1. "Compiler not found"

**المشكلة:** GCC/G++ أو Python غير مثبت

**الحل:**

#### Windows
```bash
# تثبيت MinGW (GCC/G++)
# قم بتحميل من: https://sourceforge.net/projects/mingw/
# أو استخدم Chocolatey:
choco install mingw

# تثبيت Python
# قم بتحميل من: https://www.python.org/downloads/
# أو:
choco install python
```

#### Linux
```bash
sudo apt update
sudo apt install build-essential python3
```

#### Mac
```bash
xcode-select --install
brew install python3
```

---

### 2. "Execution failed"

**الأسباب المحتملة:**
- خطأ في الكود
- خطأ في التجميع (Compilation error)
- خطأ في وقت التشغيل (Runtime error)

**الحل:**
- تحقق من رسالة الخطأ في Output tab
- الآن الأخطاء تظهر بوضوح!

---

### 3. "Time limit exceeded"

**المشكلة:** الكود يأخذ وقت طويل جداً (أكثر من 5 ثواني)

**الحل:**
- تحسين الخوارزمية
- تجنب الحلقات اللانهائية
- استخدام خوارزميات أسرع

---

### 4. خطأ في التجميع (Compilation Error)

**مثال:**
```
error: expected ';' before '}' token
```

**الحل:**
- اقرأ رسالة الخطأ بعناية
- تحقق من السطر المذكور في الخطأ
- تأكد من:
  - الفواصل المنقوطة `;`
  - الأقواس `{}` `()` `[]`
  - أسماء المتغيرات صحيحة

---

## 📝 أمثلة

### مثال 1: Python بسيط
```python
n = int(input())
print(f"Hello {n}!")
```

**Input:**
```
5
```

**Output:**
```
Hello 5!
```

---

### مثال 2: C++ بسيط
```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    cout << "Hello " << n << "!" << endl;
    return 0;
}
```

**Input:**
```
5
```

**Output:**
```
Hello 5!
```

---

### مثال 3: C بسيط
```c
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    printf("Hello %d!\n", n);
    return 0;
}
```

**Input:**
```
5
```

**Output:**
```
Hello 5!
```

---

## 🎯 نصائح للاستخدام

### 1. اختبر الكود أولاً
- اكتب كود بسيط أولاً
- اختبره بـ Run
- ثم أضف المزيد من الكود

### 2. استخدم Input Tab
- أدخل البيانات في Input tab
- اضغط Run
- شاهد النتيجة في Output tab

### 3. اقرأ رسائل الخطأ
- الأخطاء الآن واضحة ومفصلة
- تحتوي على رقم السطر
- تشرح المشكلة

### 4. استخدم ChatBot
- إذا واجهت مشكلة، اسأل ChatBot
- سيساعدك في فهم الخطأ
- يعطيك اقتراحات للحل

---

## 🔍 التحقق من التثبيت

### تحقق من Python
```bash
python --version
# يجب أن يظهر: Python 3.x.x
```

### تحقق من GCC
```bash
gcc --version
# يجب أن يظهر: gcc (GCC) x.x.x
```

### تحقق من G++
```bash
g++ --version
# يجب أن يظهر: g++ (GCC) x.x.x
```

---

## 📊 معلومات تقنية

### Timeouts
- **Python/C/C++ Execution:** 5 seconds
- **Compilation:** 10 seconds

### Memory Limit
- **Max Buffer:** 1 MB

### Supported Languages
- ✅ Python 2
- ✅ Python 3
- ✅ C
- ✅ C11
- ✅ C++11
- ✅ C++14
- ✅ C++17
- ✅ C++20

---

## 🎉 التحسينات الجديدة

### ما تم إصلاحه:
1. ✅ رسائل خطأ أوضح
2. ✅ معالجة أفضل للأخطاء
3. ✅ عرض أخطاء التجميع
4. ✅ عرض أخطاء التشغيل
5. ✅ رسائل مفيدة عند عدم وجود compiler

### الآن يمكنك:
- رؤية أخطاء التجميع بوضوح
- فهم سبب فشل الكود
- الحصول على مساعدة أفضل من ChatBot

---

## 💡 أمثلة على الأخطاء

### خطأ في التجميع
```cpp
#include <iostream>
int main() {
    cout << "Hello"  // نسيت الفاصلة المنقوطة!
    return 0;
}
```

**Output:**
```
error: expected ';' before 'return'
```

### خطأ في التشغيل
```python
n = int(input())
print(10 / 0)  # قسمة على صفر!
```

**Output:**
```
ZeroDivisionError: division by zero
```

### Compiler غير موجود
```
Compiler not found. Please install GCC/G++ or Python.
```

---

## 🚀 الخطوات التالية

1. جرب الأمثلة أعلاه
2. اختبر الكود الخاص بك
3. إذا واجهت مشكلة، اسأل ChatBot
4. استمتع بالبرمجة! 🎉

---

**تاريخ التحديث:** 2025-11-08  
**الحالة:** ✅ محسّن ويعمل بشكل ممتاز
